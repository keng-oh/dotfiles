# YYYY-MM-DD `<変更した対象のパス>`

<!--
doc/system-changes/ に置く1変更ぶんの記録テンプレート。
このファイルをコピーして `YYYY-MM-DD-<対象を表す短い名前>.md` にリネームして使う。
例: 2026-08-28-journald-sync.md

HTMLコメント(この説明)は書き終えたら消すこと。
-->

**状態:** 適用済み / 適用待ち / 撤去済み のいずれか

<!-- 撤去した場合も、このファイルは消さず状態だけ変える。
     「試して駄目だった」記録には、同じ道を再び辿らせない価値がある。 -->

## 内容

<!-- 実際に書き込んだ値と、適用に使ったコマンドをそのまま貼る。
     後から一字一句同じものを再現できる状態にしておく。 -->

```
```

## 理由

<!-- なぜこの変更が必要だったか。
     数か月後の自分や、文脈を持たない別セッションのアシスタントが読んで
     「その判断は妥当だ」と再現できる程度に書く。
     既定値がどうなっていて何が問題だったか、まで踏み込む。
     関連する調査メモがあれば doc/ 内の該当ファイルを参照する。 -->

## 元に戻す方法

<!-- バックアップファイルのパス、削除すべきファイル、再生成が必要なコマンドなど。
     恒久化ではなく実験だった場合は「いつ見直すか」の目安も書く。 -->

```bash
```

## 検証方法

<!-- 適用されているかを確認する手順。
     root権限なしで確認できる方法を優先する(sudoが要ると確認自体が面倒になるため)。
     設定ファイルの内容と、実際に効いているかの両方を確認できるとよい。 -->

```bash
```

## 注意

<!-- 副作用、放置するとまずいこと、定期的な後始末が必要ならその手順。
     なければこの節ごと削除してよい。 -->
